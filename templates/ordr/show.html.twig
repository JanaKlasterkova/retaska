{% extends 'base.html.twig' %}

{% block title %}Ordr{% endblock %}

{% block body %}
    <h1>Ordr</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordr.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ordr.Name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ ordr.Email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ ordr.Phone }}</td>
            </tr>
            <tr>
                <th>Street</th>
                <td>{{ ordr.Street }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ ordr.City }}</td>
            </tr>
            <tr>
                <th>PSC</th>
                <td>{{ ordr.PSC }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ ordr.county.Name }}</td>
            </tr>
            <tr>
                <th>Doprava</th>
                <td>{{ ordr.doprava.Name }}</td>
            </tr>
            <tr>
                <th>Platba</th>
                <td>{{ ordr.platby.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ordr_index') }}">back to list</a>

    <a href="{{ path('ordr_edit', {'id': ordr.id}) }}">edit</a>

    {{ include('ordr/_delete_form.html.twig') }}
{% endblock %}
